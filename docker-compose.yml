services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: magic-resume:local-opencode
    container_name: magic-resume
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
